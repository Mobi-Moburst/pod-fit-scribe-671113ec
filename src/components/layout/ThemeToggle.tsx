// Theme toggle removed — the Moburst Intercept design system is dark-only
// (ThemeProvider forces the dark theme). Kept as a no-op so existing
// imports/usages continue to render nothing.
export const ThemeToggle = () => null;
